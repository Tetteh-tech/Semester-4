import React from 'react';
import AuthNavigator from '../../navigator/AuthNavigator';

const App = () => {
  return <AuthNavigator />; // i Used only the navigator here to display my screens 
};

export default App;
